# NATS Server Configuration with MQTT support

server_name: nats-dev

# Standard NATS port
port: 4222

# HTTP monitoring port
http_port: 8222

# MQTT listener
mqtt {
  port: 1883
}

# Enable JetStream (required for MQTT QoS persistence)
jetstream {
  store_dir: "./data"
}
